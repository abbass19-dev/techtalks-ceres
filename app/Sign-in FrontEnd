"use client";

import { useState } from "react";

export default function SignInPage() {
  const [email, setEmail] = useState("");
  const [password, setPassword] = useState("");
  const [showPassword, setShowPassword] = useState(false);
  const [loading, setLoading] = useState(false);

  const handleSubmit = async () => {
    setLoading(true);
    await new Promise((r) => setTimeout(r, 1500));
    setLoading(false);
  };

  return (
    <>
      <style>{`
        @import url('https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;0,600;1,300;1,400;1,500&family=DM+Sans:wght@300;400;500&display=swap');

        *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
        html, body { height: 100%; }

        .root {
          display: grid;
          grid-template-columns: 1.1fr 0.9fr;
          min-height: 100vh;
          font-family: 'DM Sans', sans-serif;
          overflow: hidden;
        }

        /* ══ LEFT PANEL ══ */
        .left {
          background: #0d1f0d;
          position: relative;
          overflow: hidden;
          display: flex;
          flex-direction: column;
          justify-content: space-between;
          padding: 3rem;
        }

        .glow-a {
          position: absolute;
          width: 600px; height: 600px;
          border-radius: 50%;
          background: radial-gradient(circle, rgba(90,140,60,.25) 0%, transparent 70%);
          bottom: -180px; left: -160px;
          pointer-events: none; z-index: 0;
        }
        .glow-b {
          position: absolute;
          width: 350px; height: 350px;
          border-radius: 50%;
          background: radial-gradient(circle, rgba(201,168,76,.09) 0%, transparent 70%);
          top: -80px; right: -80px;
          pointer-events: none; z-index: 0;
        }

        .left-top { position: relative; z-index: 1; }

        .wordmark {
          font-family: 'Cormorant Garamond', serif;
          font-size: 1.8rem;
          font-weight: 500;
          letter-spacing: .28em;
          color: #e8d5a0;
          text-transform: uppercase;
        }
        .wordmark-accent { color: #C9A84C; }

        .sub-wordmark {
          font-size: 10px;
          letter-spacing: .22em;
          text-transform: uppercase;
          color: #4a7a4a;
          margin-top: 5px;
          font-weight: 400;
        }

        .left-mid {
          position: relative;
          z-index: 1;
          flex: 1;
          display: flex;
          flex-direction: column;
          justify-content: center;
          padding: 1.5rem 0;
        }

        .eyebrow {
          font-size: 10px;
          letter-spacing: .28em;
          text-transform: uppercase;
          color: #4a7a4a;
          margin-bottom: 1.1rem;
        }

        .headline {
          font-family: 'Cormorant Garamond', serif;
          font-size: 3.8rem;
          font-weight: 300;
          line-height: 1.08;
          color: #f0ebe0;
          margin-bottom: 1.5rem;
        }
        .headline-italic {
          font-style: italic;
          color: #C9A84C;
        }

        .body-text {
          font-size: 13px;
          font-weight: 300;
          line-height: 1.9;
          color: #5a8a5a;
          max-width: 320px;
          letter-spacing: .02em;
        }

        .stats {
          display: flex;
          gap: 2rem;
          margin-top: 2.2rem;
        }
        .stat {
          border-left: 1px solid rgba(201,168,76,.3);
          padding-left: 1rem;
        }
        .stat-num {
          font-family: 'Cormorant Garamond', serif;
          font-size: 1.8rem;
          font-weight: 400;
          color: #e8d5a0;
          line-height: 1;
        }
        .stat-label {
          font-size: 10px;
          letter-spacing: .15em;
          text-transform: uppercase;
          color: #3a6a3a;
          margin-top: 4px;
        }

        .pills {
          display: flex;
          flex-wrap: wrap;
          gap: 7px;
          margin-top: 2rem;
        }
        .pill {
          padding: 5px 13px;
          border: 1px solid rgba(201,168,76,.2);
          border-radius: 100px;
          font-size: 10.5px;
          color: #7aaa7a;
          letter-spacing: .06em;
          background: rgba(201,168,76,.04);
        }

        .left-bottom { position: relative; z-index: 1; }

        .divider-mark {
          display: flex;
          align-items: center;
          gap: 10px;
          margin-bottom: 1rem;
        }
        .divider-mark::before, .divider-mark::after {
          content: '';
          flex: 1;
          height: .5px;
        }
        .divider-mark::before { background: linear-gradient(90deg, transparent, rgba(201,168,76,.3)); }
        .divider-mark::after  { background: linear-gradient(90deg, rgba(201,168,76,.3), transparent); }

        .copyright {
          font-size: 10px;
          letter-spacing: .12em;
          color: #2d5a2d;
          text-transform: uppercase;
        }

        /* ══ RIGHT PANEL ══ */
        .right {
          background: #fafaf7;
          display: flex;
          align-items: center;
          justify-content: center;
          padding: 3rem 2.5rem;
          position: relative;
        }
        .right::before {
          content: '';
          position: absolute;
          inset: 0;
          background-image:
            linear-gradient(rgba(201,168,76,.045) 1px, transparent 1px),
            linear-gradient(90deg, rgba(201,168,76,.045) 1px, transparent 1px);
          background-size: 32px 32px;
          pointer-events: none;
        }

        .corner {
          position: absolute;
          width: 64px; height: 64px;
          pointer-events: none;
        }
        .corner-tl { top: 20px; left: 20px; border-top: 1px solid rgba(201,168,76,.3); border-left: 1px solid rgba(201,168,76,.3); }
        .corner-br { bottom: 20px; right: 20px; border-bottom: 1px solid rgba(201,168,76,.3); border-right: 1px solid rgba(201,168,76,.3); }

        .form-wrap {
          width: 100%;
          max-width: 360px;
          position: relative;
          z-index: 1;
          animation: fadeUp .6s cubic-bezier(.22,1,.36,1) both;
        }
        @keyframes fadeUp {
          from { opacity: 0; transform: translateY(20px); }
          to   { opacity: 1; transform: translateY(0); }
        }

        .badge-wrap {
          display: flex;
          align-items: center;
          gap: 10px;
          margin-bottom: 1.3rem;
        }
        .badge {
          width: 24px; height: 24px;
          border-radius: 50%;
          background: rgba(201,168,76,.1);
          border: 1px solid rgba(201,168,76,.35);
          display: flex;
          align-items: center;
          justify-content: center;
          font-size: 10px;
          color: #C9A84C;
          font-family: 'Cormorant Garamond', serif;
          font-weight: 500;
          flex-shrink: 0;
        }
        .badge-line {
          height: .5px;
          flex: 1;
          background: linear-gradient(90deg, rgba(201,168,76,.3), transparent);
        }

        .form-eyebrow {
          font-size: 10px;
          letter-spacing: .25em;
          text-transform: uppercase;
          color: #9aaa6a;
          margin-bottom: .6rem;
        }
        .form-title {
          font-family: 'Cormorant Garamond', serif;
          font-size: 2.8rem;
          font-weight: 400;
          color: #0d1a0d;
          line-height: 1.1;
          margin-bottom: .3rem;
        }
        .form-sub {
          font-size: 12.5px;
          color: #999;
          font-weight: 300;
          margin-bottom: 2.2rem;
        }

        .field { margin-bottom: 1.2rem; position: relative; }

        .fl-wrap { position: relative; }

        .fl-label {
          position: absolute;
          left: 0;
          top: 14px;
          font-size: 11px;
          letter-spacing: .18em;
          text-transform: uppercase;
          color: #bbb;
          transition: all .2s;
          pointer-events: none;
          font-weight: 500;
        }

        .fl-input {
          width: 100%;
          background: transparent;
          border: none;
          border-bottom: 1px solid #ddd;
          padding: 14px 0 8px;
          font-family: 'DM Sans', sans-serif;
          font-size: 14.5px;
          font-weight: 300;
          color: #0d1a0d;
          outline: none;
          transition: border-color .25s;
        }

        .fl-input:focus { border-bottom-color: #C9A84C; }

        .fl-input:focus ~ .fl-label,
        .fl-input:not(:placeholder-shown) ~ .fl-label {
          top: -2px;
          font-size: 9px;
          color: #C9A84C;
          letter-spacing: .22em;
        }

        .underline-anim {
          position: absolute;
          bottom: 0; left: 0;
          height: 1px;
          width: 0;
          background: #C9A84C;
          transition: width .35s ease;
        }
        .fl-input:focus ~ .underline-anim { width: 100%; }

        .pw-row { position: relative; }
        .pw-eye {
          position: absolute;
          right: 0;
          top: 50%;
          transform: translateY(-20%);
          background: none;
          border: none;
          cursor: pointer;
          font-size: 10px;
          font-weight: 500;
          letter-spacing: .1em;
          text-transform: uppercase;
          color: #C9A84C;
          font-family: 'DM Sans', sans-serif;
          padding: 0;
        }

        .opts {
          display: flex;
          justify-content: space-between;
          align-items: center;
          margin: 1.2rem 0 1.8rem;
        }
        .chk-label {
          display: flex;
          align-items: center;
          gap: 6px;
          font-size: 12px;
          color: #777;
          cursor: pointer;
          user-select: none;
        }
        .chk-label input { accent-color: #C9A84C; width: 13px; height: 13px; }

        .forgot-lnk {
          font-size: 12px;
          color: #9aaa6a;
          text-decoration: none;
          font-weight: 500;
          transition: color .15s;
        }
        .forgot-lnk:hover { color: #C9A84C; }

        .btn-signin {
          width: 100%;
          padding: 15px;
          background: #0d1f0d;
          color: #e8d5a0;
          border: none;
          font-family: 'DM Sans', sans-serif;
          font-size: 11px;
          font-weight: 500;
          letter-spacing: .25em;
          text-transform: uppercase;
          cursor: pointer;
          position: relative;
          overflow: hidden;
          transition: background .25s;
        }
        .btn-signin:hover { background: #1a3a1a; }
        .btn-signin::after {
          content: '';
          position: absolute;
          inset: 0;
          background: linear-gradient(105deg, transparent 38%, rgba(201,168,76,.18) 50%, transparent 62%);
          transform: translateX(-100%);
          transition: transform .55s;
        }
        .btn-signin:hover::after { transform: translateX(100%); }
        .btn-signin:disabled { background: #2a3a2a; pointer-events: none; }

        .or-div {
          display: flex;
          align-items: center;
          gap: 10px;
          margin: 1.3rem 0;
        }
        .or-div::before, .or-div::after { content: ''; flex: 1; height: .5px; background: #e0e0d8; }
        .or-div span { font-size: 10px; color: #bbb; letter-spacing: .18em; text-transform: uppercase; }

        .btn-google {
          width: 100%;
          padding: 13px;
          background: #fff;
          border: 1px solid #e0e0d8;
          font-family: 'DM Sans', sans-serif;
          font-size: 12.5px;
          font-weight: 400;
          color: #444;
          cursor: pointer;
          display: flex;
          align-items: center;
          justify-content: center;
          gap: 9px;
          transition: border-color .2s, background .2s;
          border-radius: 4px;
        }
        .btn-google:hover { border-color: #C9A84C; background: #fffdf5; }

        .signup-row {
          margin-top: 1.8rem;
          text-align: center;
          font-size: 12px;
          color: #aaa;
        }
        .signup-row a {
          color: #0d1f0d;
          font-weight: 500;
          text-decoration: none;
          border-bottom: 1px solid #C9A84C;
          padding-bottom: 1px;
          transition: color .15s;
        }
        .signup-row a:hover { color: #C9A84C; }

        @media (max-width: 900px) {
          .root { grid-template-columns: 1fr; }
          .left { min-height: 320px; padding: 2rem; }
          .headline { font-size: 2.6rem; }
          .right { padding: 2.5rem 1.5rem; }
        }
      `}</style>

      <div className="root">
        {/* ══ LEFT ══ */}
        <div className="left">
          <div className="glow-a" />
          <div className="glow-b" />

          <div className="left-top">
            <div className="wordmark">C<span className="wordmark-accent">É</span>RES</div>
            <div className="sub-wordmark">Nutritional Intelligence Platform</div>
          </div>

          <div className="left-mid">
            <h1 className="headline">
              Nourish<br />your body<br />with <span className="headline-italic">wisdom.</span>
            </h1>
            <p className="body-text">
              A precision nutrition guide built on ancient harvest principles and modern science. Every meal, macro, and habit — tracked beautifully.
            </p>

            <div className="stats">
              <div className="stat">
                <div className="stat-num">500+</div>
                <div className="stat-label">Food entries</div>
              </div>
              <div className="stat">
                <div className="stat-num">AI</div>
                <div className="stat-label">Powered insights</div>
              </div>
            </div>

            <div className="pills">
              <span className="pill">Macro Tracking</span>
              <span className="pill">Meal Planning</span>
              <span className="pill">Skin & Gut Health</span>
              <span className="pill">Calorie Goals</span>
              <span className="pill">Progress Reports</span>
            </div>
          </div>

          <div className="left-bottom">
            <div className="divider-mark">
              <svg width="14" height="14" viewBox="0 0 24 24" fill="none">
                <path d="M12 2C12 2 8 6 8 12C8 15 10 18 12 19C14 18 16 15 16 12C16 6 12 2 12 2Z" stroke="#C9A84C" strokeWidth="1.2" fill="none" />
                <path d="M12 19V22" stroke="#C9A84C" strokeWidth="1.2" strokeLinecap="round" />
                <path d="M9.5 10Q12 8.5 14.5 10" stroke="#C9A84C" strokeWidth=".9" fill="none" strokeLinecap="round" />
              </svg>
            </div>
            <div className="copyright">© 2026 Céres · All rights reserved</div>
          </div>
        </div>

        {/* ══ RIGHT ══ */}
        <div className="right">
          <div className="corner corner-tl" />
          <div className="corner corner-br" />

          <div className="form-wrap">
            <div className="badge-wrap">
              <div className="badge">01</div>
              <div className="badge-line" />
            </div>

            <div className="form-eyebrow">Member access</div>
            <h2 className="form-title">Sign in</h2>
            <p className="form-sub">Continue your nourishment journey</p>

            {/* Email */}
            <div className="field">
              <div className="fl-wrap">
                <input
                  className="fl-input"
                  type="email"
                  id="email"
                  placeholder=" "
                  value={email}
                  onChange={(e) => setEmail(e.target.value)}
                  autoComplete="email"
                />
                <label className="fl-label" htmlFor="email">Email address</label>
                <span className="underline-anim" />
              </div>
            </div>

            {/* Password */}
            <div className="field">
              <div className="fl-wrap pw-row">
                <input
                  className="fl-input"
                  type={showPassword ? "text" : "password"}
                  id="password"
                  placeholder=" "
                  value={password}
                  onChange={(e) => setPassword(e.target.value)}
                  autoComplete="current-password"
                  style={{ paddingRight: "48px" }}
                />
                <label className="fl-label" htmlFor="password">Password</label>
                <span className="underline-anim" />
                <button className="pw-eye" type="button" onClick={() => setShowPassword(!showPassword)}>
                  {showPassword ? "Hide" : "Show"}
                </button>
              </div>
            </div>

            <div className="opts">
              <label className="chk-label">
                <input type="checkbox" /> Remember me
              </label>
              <a href="/forgot-password" className="forgot-lnk">Forgot password?</a>
            </div>

            <button
              className="btn-signin"
              onClick={handleSubmit}
              disabled={loading}
              type="button"
            >
              {loading ? "Signing in..." : "Sign in →"}
            </button>

            <div className="or-div"><span>or continue with</span></div>

            <button className="btn-google" type="button">
              <svg width="15" height="15" viewBox="0 0 24 24">
                <path d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z" fill="#4285F4"/>
                <path d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z" fill="#34A853"/>
                <path d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l3.66-2.84z" fill="#FBBC05"/>
                <path d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z" fill="#EA4335"/>
              </svg>
              Continue with Google
            </button>

            <p className="signup-row">
              Don&apos;t have an account? <a href="/sign-up">Create one</a>
            </p>
          </div>
        </div>
      </div>
    </>
  );
}
